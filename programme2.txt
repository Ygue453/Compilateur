PROGRAMME exemple;
    VAR nb;
    DEBUT
        ECRIRE('début...');
        LIRE(nb);
        nb := nb + 2 + 10;
        ECRIRE('la valeur de la variable + 2 est : ', nb);
        ECRIRE();
    FIN.